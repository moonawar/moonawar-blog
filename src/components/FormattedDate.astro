---
interface Props {
    date: Date
}

const { date } = Astro.props;
const formattedDate = date.toLocaleDateString('en-US', {
    weekday: 'long',
    year: 'numeric',
    month: 'short',
    day: 'numeric'
});
---

<time class="text-xs">{ formattedDate }</time>